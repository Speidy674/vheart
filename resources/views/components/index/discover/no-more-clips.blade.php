<div class="text-center py-12 space-y-2">
    <p class="font-medium">{{ __('index.clips.no-more') }}</p>
    <x-ui.button variant="ghost" href="{{ route('submitclip.create') }}" class="text-sm">{{ __('index.clips.submit') }}</x-ui.button>
</div>
